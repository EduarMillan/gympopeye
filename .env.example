# Copia este archivo como `.env.local` y pega tus claves de Supabase.
# (Settings → API en el panel de Supabase). `.env.local` no se sube a git.
# Si dejas esto vacío, la app funciona offline y solo se desactiva la sync.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
